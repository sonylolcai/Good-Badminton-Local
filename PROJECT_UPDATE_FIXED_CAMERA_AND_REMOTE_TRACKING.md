# 项目更新：固定机位与远端球员追踪

## 生命周期

- 状态：进行中
- 阶段：视觉分析基础能力验证
- 适用范围：Good-Badminton 及未来 AI 羽毛球赛后复盘与训练助手
- 固定约束：后续产品、开发、测试和部署均以**固定摄像头、固定焦距、固定画面裁切**为前提；移动机位仅作为未来备用能力，不进入当前验收范围。

## 会话总结

### 已完成

1. 已完成动态球场模板、视频候选帧回退、手动四角标定、模板失配保护、完整输出视频、热力图与散点图、WebUI 后台日志等基础能力，并已合并到本地 `main`。
2. 已确认固定机位不需要每场重新检测球场四角。首次标定后保存机位配置，后续比赛只抽样校验画面是否发生位移、变焦、裁切或宽高比变化。
3. 已确认当前球场映射使用双打完整场地 `6.10m × 13.40m`；单打仅限制有效边线至 `5.18m`，不需要为单双打重做摄像头标定。
4. 已完成当前 852×480、约 9 分钟完整输出的连续性复核：全画面1280、阈值0.25时，远端球员真实检测覆盖率为89.98%（缺1703/17002帧，最长连续缺69帧/2.3秒），近端为99.39%；羽毛球实际接受覆盖率仅43.32%（最长连续缺134帧/4.47秒）。早期12帧小样本指标只能用于方向筛选，不能替代完整时间序列统计。
5. 已确认远端漏检包含两类原因：一类是远端球员站到底线外后被统一0.75米球场边界误删；另一类是近远球员在固定视角中重叠，远端人体被大面积或完全遮挡，模型没有独立候选。前者已通过“横向0.75米、远端底线3米”的非对称过滤修复；后者必须依赖带状态的时序跟踪，不能伪装为真实检测。
6. 已形成双打架构方向：公共视觉引擎复用，球员身份由 `track_id` 驱动；不再以 `upper/lower` 作为个人身份。

## 当前问题

### P0：远端球员无法稳定追踪

这是当前最高优先级问题，也是双打的前置阻塞项。远端球员在低分辨率视频和默认 640 输入下会漏检；一旦人体候选缺失，后续姿态、身份、移动轨迹和击球归属均无从计算。双打会增加四人遮挡、交叉、前后轮转和身份互换，使该问题进一步放大。

### P0：固定机位配置尚未产品化

现有能力仍需要围绕视频模板和角点工作。固定场馆机位需要可命名、可复用、可校验的标定配置，且不得在新比赛中静默覆盖既有标定。

### P1：当前球员状态模型仅支持单打

`PlayerTracker` 固定维护 `upper/lower` 两个人；上半场多人会被丢弃，无法承载双打的四人身份、轨迹、热力图、统计和换边。

### P1：羽毛球连续轨迹不稳定

远端小目标、高速拖影和低分辨率会造成球检测断裂。完整视频实际接受覆盖率约43.3%，说明问题不只是偶发闪烁。简单降低置信度会引入白线、灯光和鞋面等误检，不能作为首选方案。

## 处理方案

### 固定机位优先

每个场馆/场地建立独立机位配置，保存参考图、归一化四角、ROI、透视矩阵、参考分辨率、宽高比和校验阈值。新比赛仅抽样 3～5 帧校验；校验失败时停止分析并要求重新标定。

### 远端球员 P0 检测与追踪方案

采用固定机位非对称推理：全场以 640 进行基础检测，远端半场裁剪并放大后再次以 640 推理，结合 NMS、人体尺寸、ROI 和球场坐标合并结果。并保留全画面 1280 的高精度模式用于离线基线比较。

人体位置计算要支持降级：双脚踝可见时取中点；仅一只脚踝时取单脚；双脚踝均缺失时取人体框底边中心，并记录较低的位置置信度。

在检测稳定后接入 ByteTrack，短暂漏检只能产生带置信度的预测轨迹，必须与真实检测区分。以 `track_id` 维持个人身份，为双打四人跟踪、遮挡恢复和人工身份确认建立基础。

当前低清固定机位将 YOLO Pose 默认阈值设为0.15，并保留WebUI/CLI可配置入口；同一半场出现多个候选时，优先选择与上一帧位置最连续的人。四段顽固缺失区间重放中，缺失帧从190降到129，主要收益来自修复远端底线过滤；仍然存在的长缺口经逐帧检查属于人体重叠遮挡。

### 模型策略

当前模型作为基线，不立即训练或替换。优先顺序为：原始 1080P/4K 视频验证 → 推理尺寸 A/B 测试 → 固定机位远端分区检测 → 后处理降级与时间跟踪 → 固定测试集评测。以上措施完成后仍不达标，才收集固定机位失败片段，标注人体框/关键点或球框，并进行微调。

每次变更都必须在同一套训练、验证、独立测试集上比较；测试集不得参与训练或调参。

## TODO

### P0

- [ ] 实现可命名的固定机位配置及抽样校验；所有新流程默认读取固定机位配置。
- [x] YOLO Pose 新增 640/960/1280 `imgsz` 配置，运行元数据保存实际模型、阈值、设备、ROI、输入尺寸和来源；球模型的同类改造仍留在 P1。
- [x] 实现“全场 640 + 远端 ROI 640”及跨来源 NMS，并与全场 640/1280 建立可重复评测。
- [x] 人体落点支持双脚中点、单脚和框底中心，输出真实/降级/缺失状态与置信度。
- [x] 建立当前 852×480 和 1280×720 的 preliminary 基线集与评测脚本；每段12个视觉复核样本，仅用于工程方向选择。
- [x] 输出远端召回率、漏检率、最长连续漏检、CPU单帧耗时及误检样本。
- [x] 将人体阈值开放为WebUI/CLI参数，低清固定机位默认0.15；球场过滤改为横向与远/近底线独立容差，并使用上一帧位置消解同半场多候选。
- [ ] 将每段扩充至至少20个连续人工复核样本，并增加1080P/4K与双打视频；人工复核前不得关闭 ByteTrack 前置门槛。

初步固定机位结果（紧致标定 ROI）：全场640汇总召回率0%、约60.0ms/帧；全场1280为95.8%、约136.0ms/帧；全场640+远端ROI 640为87.5%、约118.8ms/帧。当前仍推荐全场1280作为真实检测基线，但完整视频存在最长2.3秒远端缺失，且典型顽固帧为完全遮挡，因此尚未达到 ByteTrack正式接入和双打扩展门槛。

### P1

- [x] 新增球场坐标驱动的 `FixedCameraMatchPipeline`：独立输出多球员 `track_id`、瞬时 `zone_id`、赛后身份认领接口、单目近似三维球与可拒绝回合状态；旧 `PlayerTracker.upper/lower` 仅保留给现有 UI 兼容。详见 `FIXED_CAMERA_SINGLES_SPATIAL_TRACKING.md`。
- [ ] 将现有热力图、散点图和视频轨迹叠加迁移到 `spatial.tracks` 的 `track_id` 聚合视图；不得继续以 `upper/lower` 作为新功能的数据源。
- [ ] 建立遮挡片段人工真值，评测 ByteTrack 的遮挡恢复、ID-switch和最长可可信预测时间；通过门槛后再接入正式流程。
- [ ] 增加 `singles | doubles`、队伍归属、四人身份确认、换边保持和人工纠错。
- [ ] 使 detections、轨迹、热力图和统计支持个人、队伍、全场三层聚合。
- [x] 为球检测实现最多3帧的短时恒速预测，以及 `detected / predicted / missing`、置信度、候选数量和过滤原因；预测只用于显示/导出，不作为击球或失误真值。
- [ ] 为球检测建立逐帧人工真值并评测远端分区或专用时序模型，将真实检测覆盖率从当前约43.3%提升到可支持击球分析的门槛。

### P2

- [ ] 在固定测试集上比较高分辨率/分区方案、较大 YOLO Pose、RTMPose/RTMO；不以模型名称决定替换。
- [ ] 当 ByteTrack 的 ID-switch 超过门槛时，对比 BoT-SORT；遮挡恢复仍不足才评估 ReID。
- [ ] 只有 P0/P1 措施完成且指标仍不达标时，建立固定机位标注集并微调模型。

### P3：任务 D — 本地流式回放验证（仅记录，暂不实施）

- [ ] 为本地开发模式增加“完整录像按 1～2 秒 fMP4 分片实时投送”的回放脚本；它必须复用未来
  `stream-sessions` 的分片协议，而不是把完整文件接口包装成“流式”。
- [ ] 每个分片保存 `session_id`、`segment_index`、源视频开始/结束时间、SHA-256、接收确认和
  队列/处理状态；相邻分片的 Track ID 状态必须连续，服务重启后只能恢复或标记
  `interrupted_needs_rebuild`。
- [ ] 回放报告分别统计 1080p 30 FPS/60 FPS 下每个分片的 P50/P95、积压、GPU/显存、轨迹中断和
  封口后派生数据耗时；未通过这些指标前，不将完整上传批处理宣传为实时流式能力。

## 完成标准

1. 固定机位的新比赛通过抽样校验后无需重新标定。
2. 远端球员检测与追踪在固定测试集上达到预先定义的验收阈值，并有新旧方案量化对比。
3. 所有轨迹点均可追溯为真实检测或带置信度的预测。
4. 双打开始前，四名球员均能获得可持续的 `track_id`、稳定队伍归属和可人工修正的身份绑定。

## 关联文档

- `DOUBLES_SUPPORT_ARCHITECTURE.md`
- `PROJECT_ANALYSIS_zh.md`
