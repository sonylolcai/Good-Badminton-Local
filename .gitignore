# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/
.agents/

# Local inputs and generated outputs
results/
outputs/
api_data/
.gpu-api.env
.gpu-api.local.env
.webui-remote-gpu.env
.webui-operator.env
.env.development
.env.production
business_gateway/fixed_video_catalog.local.json
*.log
# Local deployment artifact: upload it to the GPU provider, never commit it.
deploy/good-badminton-gpu-api-upload.zip
# Deployment archives are generated from the tracked scripts. The TrackNet
# archive and Ultralytics checkout are third-party inputs, not project source.
deploy/*.zip
# Release bundles are generated from tracked deployment scripts; keep them
# locally for upload, never in Git history.
deploy/release/*.zip
Ultralytics/

# Model weights are intentionally not published
weights/*
!weights/.gitkeep
*.pt
*.pth
*.onnx

# Local, potentially large training datasets are reproduced from their
# separately retained source annotations rather than checked into Git.
datasets/

# OS and editor files
.DS_Store
Thumbs.db
.idea/
.vscode/
