# 赛后运动表现分析：结论、计划与数据规范

## 1. 产品目标

第一阶段以固定机位的整场比赛评价为主，不要求用户逐回合回看，但后端必须提前识别和保存回合边界。最终目标不是只生成标注视频和热力图，而是形成可追溯的证据链：

```text
视频帧
→ 球员与羽毛球检测
→ 多目标跟踪和球场坐标
→ 回合与击球事件
→ 技战术指标
→ 运动员能力评分与可信度
```

## 2. 已确定的技术结论

### 固定机位与球场

- 固定摄像头、焦距和画面裁切时，球场只需首次标定一次，后续比赛抽样校验即可。
- 标定始终使用双打外侧边线，对应 `6.10m × 13.40m`；单打只改变有效边线，不重新标定。
- 新视频只抽样 3～5 帧验证位移、变焦、裁切和宽高比，验证失败时要求重新标定。

### 采样频率

- 原视频帧率保持不变；采样频率只控制模型推理，不重新编码源视频。
- 球员位置第一版固定为 `10Hz`：30FPS 每3帧一次，60FPS 每6帧一次；29.97/59.94FPS 或可变帧率视频按时间戳调度。
- 10Hz 足以支持整场热力图、场区占比、移动距离、平均速度、回位、覆盖率和基础站位分析。
- 精细步法、挥拍和击球瞬间不能只依靠10Hz。未来在疑似击球前后局部回看原始帧，临时提高到20～30Hz。
- 羽毛球使用独立采样策略。第一版可采用10～15Hz辅助回合识别；需要球路、落点和失误类型时提高到原始帧率。

### GPU和部署

- 当前25,931帧视频在CPU上约66分钟，绝大部分耗时来自人体姿态和羽毛球模型逐帧推理。
- 租赁4090是可行路线；目标部署为 TensorRT FP16、批量推理和 NVENC。
- GPU环境已经预热后，当前视频合理目标为约2～5分钟；首次任务还需要计算容器启动、视频传输、模型加载和TensorRT Engine构建时间。
- 按量平台通常按秒计量或精确到秒结算，不代表每次启动强制支付一小时；但费用按实例运行时间而非GPU实际利用率计算。

### 回合数据

- 前端暂时只展示整场报告，不必先实现逐回合播放器。
- 后端必须保存 `rally_id`、开始/结束时间、持续时间、结束原因和识别置信度。
- 当前通过检测记录帧号空缺切分回合的方式不可靠，应改成独立事件状态机。
- 失误率、回合数、平均拍数、最后击球者和回合结束原因都依赖回合层，不能完全推迟。

## 3. 推荐处理管线

```text
所有原始帧：读取、保留时间戳、必要时输出标注视频

球员位置：10Hz真实检测
  └─ ByteTrack在原始时间轴上维持track_id和短时预测

羽毛球第一版：10～15Hz检测
  └─ 用于球是否活跃、过网方向和回合边界的弱证据

羽毛球增强版：原始帧率检测
  └─ 击球、球路、落点、出界、下网和失误归因

疑似击球窗口：回看前后0.5秒原始帧
  └─ 20～30Hz姿态、手腕和未来的球拍分析
```

任何插值或跟踪生成的位置必须保存 `predicted` 状态和置信度，不能作为真实检测伪装写入。

## 4. 数据分层

### A. 球员时间序列

建议固定10Hz，保存：

- 时间戳和源帧号
- `track_id`、队伍和球员身份
- 图像坐标、球场坐标、速度和加速度
- 身体朝向、姿态质量、跟踪状态和置信度
- 当前场区、移动阶段和是否正在回位

### B. 羽毛球时间序列

建议独立频率，保存：

- 图像坐标和球场三维坐标 `(x, y, z)`
- 速度向量、速度大小和轨迹状态
- `detected / predicted / missing`
- 当前 `shot_id`、最后击球者和检测置信度

### C. 击球事件

每次击球保存：

- 击球时间、球员、击球位置和身体位置
- 球路类型、起始/目标/实际落点、过网高度
- 击球前准备时间、击球后回位时间、受压程度
- 结果：继续、制胜分、下网、出界、未触球
- 失误归因和事件置信度

### D. 回合数据

每个回合保存：

- 开始和结束时间
- 拍数、持续时间、最后击球者
- 获胜者、结束原因和是否为受迫失误
- 回合边界置信度与人工修正状态

### E. 整场评分

评分必须同时输出样本量和可信度，建议包括：

- 场地覆盖与站位
- 移动效率和回位
- 进攻质量
- 防守稳定性
- 击球选择
- 主动得分率
- 受迫/非受迫失误率
- 多拍稳定性
- 数据完整率

## 5. 10秒模拟数据

样例目录：`samples/simulated_rally_10s/`

| 文件 | 频率/粒度 | 用途 |
|---|---|---|
| `player_tracking_10hz.csv` | 10Hz，共101个时间点 | 两名球员跑位、速度、加速度、朝向和跟踪质量 |
| `shuttlecock_tracking_30hz.csv` | 30Hz，共301个时间点 | 羽毛球三维轨迹、速度、击球段和检测状态 |
| `events.jsonl` | 每次击球一条 | 12次击球、球路、受压、质量、结局和失误归因 |
| `rally_summary.json` | 每回合一条 | 回合结果、派生指标、示例评分和可信度 |
| `README.md` | 说明 | 字段解释、能力边界和使用建议 |

该样例是用于验证数据合同与评分逻辑的合成数据，不是模型实际输出，也不能用于声称某位真实球员的能力。

## 6. 能否据此评价运动员

### 可以从整场数据可靠评价

- 站位倾向、活动范围和场区覆盖
- 移动距离、速度区间和回位效率
- 前后场使用比例和双打站位结构
- 拍数、回合时长和多拍稳定性
- 在击球事件可靠时统计球路分布、落点分布和击球选择
- 在回合结局可靠时统计主动得分、受迫失误和非受迫失误

### 不能只靠当前位置数据评价

- 技术动作是否标准
- 拍面角度、握拍、旋转和精细发力链
- 击球质量和失误责任
- 战术选择是否正确

这些需要羽毛球连续轨迹、击球事件、球拍或更细的手臂关键点，以及比分/回合上下文。

### 10秒样本的限制

10秒样本能验证字段是否足以计算指标，也能评价这个回合中的表现；不能形成稳定的个人水平评分。正式评分至少应满足预设的有效回合数、有效击球数和检测完整率门槛。例如少于20个有效回合时，只展示原始指标和“样本不足”，不输出稳定等级。

## 7. 实施计划

### P0：建立可信数据底座

- [ ] 按时间戳实现球员10Hz采样，兼容29.97、30、50、59.94和60FPS。
- [ ] 接入 `track_id`，输出 `detected / predicted / missing` 和置信度。
- [ ] 扩展检测JSON，保存姿态质量、模型候选数和逐阶段耗时。
- [ ] 新增 `rallies.jsonl`、`events.jsonl` 和 `match_summary.json` 数据合同。
- [ ] 将整场热力图改为按时间加权，避免高FPS视频天然拥有更高密度。

### P1：整场报告第一版

- [ ] 输出场区占比、移动距离、速度、回位、覆盖率和数据完整率。
- [ ] 实现估算回合状态机，并输出边界置信度；前端只显示整场回合统计。
- [ ] 羽毛球以10～15Hz作为弱证据，不承诺精确球路、落点或失误类型。
- [ ] 修复当前回合图等于整场图的问题；若未开放逐回合功能则不生成重复图片。

### P2：击球与失误分析

- [ ] 羽毛球提高到原始帧率或事件驱动高频分析。
- [ ] 建立击球候选、最后击球者、过网、落点和回合结束原因识别。
- [ ] 将失误拆分为下网、出界、未触球，以及受迫/非受迫，全部输出置信度。
- [ ] 事件窗口回看原始帧，进行20～30Hz姿态分析。

### P3：部署与性能

- [ ] 建立4090租赁环境，导出并验证 TensorRT FP16 Engine。
- [ ] 使用批量推理和 NVENC，记录上传、冷启动、推理、渲染、编码各阶段时间。
- [ ] 任务完成后自动释放GPU，设置超时、预算和失败重试保护。
- [ ] 使用固定测试集比较CPU、PyTorch CUDA、FP16和TensorRT的速度及精度。

## 8. 评分原则

评分不是简单加权几个未经验证的数字。每个分项必须同时具备：

1. 明确的原始指标与单位；
2. 足够的有效样本量；
3. 检测和事件识别完整率；
4. 同级别、同赛制、同机位条件下建立的参考分布；
5. 可解释的扣分或加分证据；
6. 教练或标注数据验证。

模拟样例中的0～100分只是数据合同演示。正式产品应先积累有标签的比赛数据，再将原始指标映射为能力分位或等级。

## 9. 已落地的球路人工复核入口

WebUI 已提供“球路复核”Tab：从 `spatial.hit_events` 与二维羽毛球轨迹生成低置信度候选，按同一击球的时间窗口去重，并允许人工确认、修正、标为不确定或排除。原始 `detections.jsonl` 保持不可变；机器候选和人工审计记录分别保存。具体存储结构、使用步骤与训练集导出边界见 [SHOT_REVIEW_AND_ANNOTATION_WORKFLOW.md](SHOT_REVIEW_AND_ANNOTATION_WORKFLOW.md)。

## 10. 正式 Track ID、离线球路重建与双打升级（当前分支）

- `spatial.tracks` 是所有新增分析的唯一球员输入：`track_id` 为持续个体键，`zone_id`、`court_end` 只是瞬时空间事实；`team_id` 只能由赛后人工确认，不能由图像上下、左右或当前半场推断。
- 单打和双打共用 `CourtMultiObjectTracker`。双打模式最多每队两人，但不会在检测层过滤同一侧的额外球员；每条轨迹都明确标注 `detected`、`predicted` 或 `missing`，后两者不能计入检测、击球归属、得分或能力统计。
- 官方 ByteTrack 适配层已纳入，依赖为 `lap>=0.5.12`。但当前远端球员基线尚未达到启用门槛：852×480 召回不足 95%、最长漏检超过 0.30 秒、每段人工真值不足 20 帧且尚未人工二次复核。因此默认仍是球场坐标关联，只有留下通过记录的独立跟踪评测才可设置 `enable_bytetrack=True`。
- 视频完成后会写入独立派生产物：`derived/shuttle_tracks_v2.jsonl`（`detected / reconstructed / unknown_gap`）与 `derived/shot_events_v2.jsonl`。仅短且前后可约束的空洞可重建；长空洞、速度不一致和重复低置信边缘点保留为未知或拒绝，不伪造真实球轨迹。
- 球种候选会同时参考离线球轨迹、可见手部/空间接近和下一次触球的接球位置：网前到对方后场优先给“挑球/高远候选”，后场到网前给“吊球/杀球候选”，证据不足必须为 `unknown`。所有候选先进入人工复核，未确认不得用于失误率、胜负或能力评分。

### 双打验收 TODO

- [ ] 录制并人工标注正后方、侧后方或斜后方两套真实固定机位双打视频，覆盖四人、交叉、同队前后换位、遮挡和恢复。
- [ ] 运行 `evaluation/doubles/run_doubles_evaluation.py`，记录四人召回、ID-switch、最长轨迹中断、遮挡同 ID 恢复与击球归属准确率。
- [ ] 完成远端人体检测基线门槛后，单独 A/B 评测 ByteTrack；没有指标证据前，不引入 BoT-SORT 或 ReID。
- [ ] 只将跨比赛、跨清晰度、跨 FPS、跨机位人工确认的数据导出为训练集，并按完整比赛切分训练/验证/测试。
