# Good-Badminton 项目分析

## 1. 结论摘要

Good-Badminton 是一个**本地优先的羽毛球比赛视频分析原型/工具**：它把比赛视频中的球场、人和羽毛球检测结果映射到标准羽毛球场坐标，再输出带标注视频、逐帧 JSONL 数据、球员移动统计、热力图和散点图。项目提供 CLI 和 Gradio WebUI 两种入口，核心算法流程共用 `BadmintonAnalysisSystem`。

它适合个人训练复盘、算法验证、二次开发和小规模本地服务；尚不适合作为“鹰眼级判罚”或可直接商用的稳定 SaaS。主要原因是：没有精度基准与测试集、击球/技术动作仍标为实验功能、球场标定和模板匹配对机位稳定性敏感，且服务端并发、任务隔离和数据生命周期设计尚不完整。

## 2. 本次审查范围与版本

| 项目 | 结果 |
| --- | --- |
| 本地目录 | `S:\\Code Base\\MiniProgram\\bdTeach\\Good-Badminton` |
| 远程仓库 | `https://github.com/yo-WASSUP/Good-Badminton` |
| 审查提交 | `24f2208929f84f441d7af3bdb1ba6bf6c109943f`（2026-07-03） |
| 许可证 | Apache License 2.0 |
| 审查方式 | 源码、依赖、项目结构、Git 元数据静态审查 |
| 未完成的验证 | 端到端运行、模型精度和性能实测 |

限制说明：当前环境中 `python` 不在 PATH，`py.exe` 存在但没有已安装的 Python；仓库也不含 `weights/` 目录。因此未安装依赖、未下载模型、未运行推理或 WebUI。这不是运行失败结论，而是运行前置条件尚未满足。

## 3. 架构与数据流

```text
CLI (main.py) / WebUI (webui/app.py)
                │
                ▼
BadmintonAnalysisSystem (badminton_analysis/system.py)
  ├─ 球场：自动线检测或人工四角标注 → 单应性映射/ROI
  ├─ 人体：YOLO Pose 或 RTMPose / RTMO → 关键点、质心、手部点
  ├─ 羽球：YOLO 检测 → 尺寸/形状/ROI/运动连续性筛选
  ├─ 跟踪：上下半场球员归属、速度、距离、回合状态
  ├─ 可视化：骨架、球路、场地轨迹、统计叠加
  └─ 输出：H.264 MP4、metadata.json、detections.jsonl、热力图/散点图
```

关键实现：`CourtMapper` 将四个图像角点对应到 6.1m × 13.4m 的标准场地；姿态检测仅在扩展 ROI 上执行以降低计算量，而羽球仍在整帧检测；连续 5 帧被识别为球场视图时开启回合，连续 5 帧非球场视图时结束回合。WebUI 会在浏览器播放前用 FFmpeg 转码为 H.264，并只保留最近 10 个 WebUI 输出目录。

## 4. 能力评估

| 维度 | 评估 | 依据 |
| --- | --- | --- |
| 核心闭环 | 较完整 | 支持标定、检测、跟踪、可视化和结构化导出。 |
| 可用性 | 中等 | CLI 参数较全面，WebUI 支持自动检测失败后的四点人工修正。 |
| 可扩展性 | 中等 | 按 court/detection/tracking/visualization/data 分包，CLI/WebUI 共用主流程。 |
| 可验证性 | 较弱 | 未发现测试目录、CI 配置、精度报告或公开评测集。 |
| 生产就绪度 | 较弱 | 单进程、WebUI 并发限制为 1，未见鉴权、任务队列、资源配额或用户数据治理。 |
| 模型可复现性 | 中等偏弱 | 球模型必须从 Release 另行获取；RTMPose/RTMO 缺少本地权重时会尝试在线下载。 |

## 5. 主要风险与改进优先级

### P0：可信与安全边界

1. **缓存标注使用 `eval` 解析。** `system.py` 读取 `court_annotations.txt` 时执行 `eval(...)`。如果输出目录可被其他人写入，恶意文本可在本地分析进程中执行。应改为 `ast.literal_eval` 或 JSON，并对点数、顺序、图像边界做校验。
2. **不应把“鹰眼”理解为裁判级结论。** 当前代码是单目视频检测、单应性投影和启发式轨迹筛选；没有相机标定误差、球落点误差、漏检率或争议球回放证据链。产品文案应明确为“辅助分析”，不能输出判罚结论。

验收：恶意标注文件不执行任何代码并得到明确错误；为至少三个不同机位的数据集输出检测率、场地映射误差、位置误差和端到端耗时。

### P1：稳定性与工程质量

1. **缺少自动化测试与 CI。** 建议把球场映射、ROI、回合状态机、JSONL schema、轨迹异常过滤、Unicode 路径和 FFmpeg 失败回退做单元/集成测试。
2. **依赖和模型供应链不完整。** `requirements.txt` 固定了一部分包，但未锁定全部间接依赖；权重不在仓库，部分姿态模型可在线下载。应提供权重校验和、版本清单、离线安装包或首次启动检查。
3. **资源与错误恢复不足。** 长视频逐帧同步处理；WebUI 仅限制并发为 1。应增加取消、进度持久化、磁盘空间预检、GPU/CPU 显存提示及失败后可恢复的任务状态。

验收：在干净 Windows 环境按文档完成安装；固定样例能在 CPU/GPU 两种配置下跑通；CI 至少执行语法、单元测试和一个短视频 smoke test。

### P2：分析准确性与产品能力

1. 以模板相似度判断“是否比赛视图”，对转场、重放、镜头缩放、遮挡较敏感；可叠加场地关键点追踪和时序分类。
2. 羽球轨迹以像素距离、预测位置和固定阈值过滤，快速杀球、低清晰度、小球遮挡场景可能被拒绝或断轨；需要按 FPS/分辨率自适应阈值与轨迹置信度。
3. 当前“球员”按场地上下半区归类，更适合单打。双打、多人的遮挡、换边和跨中线场景需要身份关联与比赛规则层。
4. `--performance-stats` 使用 `store_true` 且默认值为 `True`，实际没有关闭开关；可改为 `BooleanOptionalAction`，提供 `--no-performance-stats`。

## 6. 建议的最小落地路线

第一阶段先完成 P0 的安全修复和一条 30–60 秒标准机位视频的端到端 smoke test；第二阶段建立标注样本与准确率指标；第三阶段再考虑批量队列、用户隔离、对象存储和 SaaS 化。若目标是教学/运动复盘，优先做“回合切分 + 移动热图 + 可下载数据”；若目标是裁判辅助，必须先补充多相机标定、误差模型和人工复核工作流。

## 7. 本地启动前提（未在本环境执行）

1. 安装 Python 3.8+ 并确保 `python --version` 可用。
2. 在项目目录创建虚拟环境后执行 `pip install -r requirements.txt`；WebUI 另执行 `pip install -r requirements-webui.txt`。
3. 从仓库 Release 下载羽球 YOLO 权重到 `weights/yolo11s-ball.pt`，或在命令中传入 `--ball-model`。
4. 安装 FFmpeg 并加入 PATH；随后先用 `python main.py --video-path videos/demo.mp4 --template-path templates/demo.png --display false` 做 smoke test。
