# 双打支持架构与跟踪方案

## 结论

项目应采用**一套公共视觉引擎 + 单打/双打模式规则层**，而不是维护两套独立系统。

单打和双打共用视频读取、球场标定、羽毛球检测、人体检测、姿态关键点、目标跟踪、轨迹渲染和结果导出。比赛模式只负责人数、身份分配、统计聚合及场上规则。

```text
公共视觉引擎
  ├─ 羽毛球检测与球路轨迹
  ├─ 人体检测与 17 个姿态关键点
  ├─ 多目标跟踪（track_id）
  └─ 球场坐标映射
       ↓
比赛模式规则
  ├─ 单打：两队各 1 人，共 2 条个人轨迹
  └─ 双打：两队各 2 人，共 4 条个人轨迹
```

## 模型与跟踪器的职责

`yolo11n-pose.pt` 和 ByteTrack 不是互相替代的模型，而是前后衔接的组件。

| 组件 | 输入 | 输出 | 职责 |
|---|---|---|---|
| `yolo11n-pose.pt` | 单帧视频 | 人体框、17 个 COCO 关键点、关键点置信度 | 找到画面中的球员、骨架和手腕位置。 |
| ByteTrack | 连续帧的人体框与检测置信度 | 稳定的 `track_id` | 将相邻帧的同一球员关联，维护个人轨迹。 |
| `yolo11s-ball.pt` | 单帧视频 | 羽毛球位置和置信度 | 识别羽毛球，并为球路和击球归属提供输入。 |
| 击球归属模块 | 球轨迹、球员关键点、`track_id` | 可能击球球员及置信度 | 结合球与手腕距离、手腕速度、人体位置和球飞行方向判断击球者。 |

## 现有实现与双打缺口

当前 `PlayerTracker` 仅维护 `upper` 和 `lower` 两名球员，属于单打数据模型。上半场出现多名球员时还会只保留一人。因此，现有逻辑无法正确保存双打四人的独立身份、轨迹和统计。

双打改造不应继续用上下半场位置作为球员身份。应使用追踪器生成的 `track_id` 代表个人；半场只用于判断队伍归属。

## 推荐架构

### 通用数据模型

核心状态应从固定的 `upper/lower` 改为以 `track_id` 为键的球员集合：

```text
Match
  ├─ mode: singles | doubles
  ├─ teams
  │   ├─ team_upper
  │   └─ team_lower
  └─ players
      ├─ track_id
      ├─ player_label / confirmed_name
      ├─ team_id
      ├─ image_position
      ├─ court_position
      ├─ keypoints
      ├─ trajectory
      ├─ visibility / tracking_confidence
      └─ movement_stats
```

单打模式限制每队一名有效球员；双打模式限制每队两名有效球员。统计、轨迹和导出逻辑均面向通用 `players` 集合，而不是分写两套业务代码。

### 双打身份规则

1. 每帧用姿态模型输出人体框、关键点和置信度。
2. 跟踪器为人体框输出连续的 `track_id`。
3. 通过球场映射判断球员当前处于哪一侧，从而分配到上半场队或下半场队。
4. 每队最多保留两条有效球员轨迹；异常的场外检测需要过滤。
5. 分析开始时由用户确认四名球员的姓名或起始站位；该绑定用于将匿名 `track_id` 映射为真实运动员。
6. 换边时更新队伍的球场半区映射，但保留个人身份和比赛队伍关系。

前后站位不是稳定身份：同一队双打队员会频繁前后轮转。因此，`front/back` 只能作为瞬时站位，不能作为长期球员 ID。

## 跟踪器选择

### 第一阶段：ByteTrack 基线

推荐组合：**YOLO Pose + ByteTrack + 球场约束**。

适用前提：固定或接近固定的全场机位、四人多数时间可见、对实时性有要求。

优势：

- 追踪开销低，容易接入现有 Ultralytics YOLO Pose 流程。
- 会在低置信度检测中进行第二轮关联，可缓解短暂遮挡造成的断轨。
- 适合先验证四人轨迹、按人统计和击球归属的完整产品链路。

限制：

- 没有外观特征（ReID），主要依赖位置、运动和框重叠。
- 两名球员网前重叠、相互交叉或长时间遮挡后，可能发生 ID 交换。

### 产品化增强：BoT-SORT + 可选 ReID

当测试表明 ByteTrack 的 ID 交换不可接受时，优先升级为：**YOLO Pose + BoT-SORT + 可选 ReID + 球场约束**。

- 固定机位：关闭相机运动补偿，减少额外计算。
- 手持或移动机位：启用相机运动补偿。
- 频繁遮挡或视觉特征明显不同：启用 ReID，利用外观特征辅助重新关联。

ReID 不应被视为双打身份问题的唯一解决方案。同队往往穿相似队服，通用 ReID 对搭档区分能力有限；用户初始确认、球场约束和轨迹连续性仍是核心保障。

### 高难视频的后续选择

在拥挤、遮挡频繁、机位移动明显且 ID 交换仍是主要问题时，可评估 Deep OC-SORT 或 TrackTrack，并以同一测试集与 ByteTrack、BoT-SORT 进行对比。不要在缺少评测数据前直接引入更多跟踪器和模型依赖。

## 实施顺序

1. 重构 `PlayerTracker`：支持最多四名球员，以 `track_id` 存储状态和历史。
2. 使用 YOLO Pose 的人体框接入 ByteTrack，并将返回的 `track_id` 与当前帧关键点绑定。
3. 增加 `singles` / `doubles` 模式，以及每队最大人数约束。
4. 增加四名球员的初始化确认界面或配置，支持人工修正身份绑定。
5. 为换边、遮挡、交叉、场外人员和漏检实现明确状态处理。
6. 将击球归属改为按 `track_id` 输出，并保存其置信度及依据。
7. 建立双打测试集后，衡量 ByteTrack；只有数据证明需要时，再升级到 BoT-SORT + ReID 或其他追踪器。

## TODO：固定机位、单双打与远端检测改进

以下任务按优先级实施。基本原则是：固定机位一次标定后长期复用；先通过原始高清视频、推理参数、分区检测和时间跟踪验证模型能力，达到明确的数据门槛后再决定是否训练模型。

### P0：固定机位标定配置

- [ ] 增加可命名的固定机位配置，至少保存参考图、归一化四角坐标、ROI、透视矩阵、参考分辨率、宽高比和校验阈值。
- [ ] 固定机位始终按双打外侧边线标定为 `6.10m × 13.40m`；单打模式只把有效宽度限制为内侧边线之间的 `5.18m`，不重新标定摄像头。
- [ ] 新比赛开始前只抽样 3～5 帧校验机位、焦距、画面裁切和宽高比，不再重新检测四角或逐帧做完整模板匹配。
- [ ] 校验失败时停止分析并提示重新标定，不静默覆盖已有机位配置。
- [ ] 保留自动选择球场帧功能，作为新机位、网络视频和移动机位的备用模式。

### P0：建立可重复的检测基线

- [x] 已建立可重复评测工具和初步小型基线：当前 852×480 与现有 1280×720 视频各 12 个逐帧视觉复核样本；报告明确标记为 preliminary，不能替代正式人工测试集。
- [ ] 扩充正式测试集：每段至少 20 个连续人工复核样本，并增加 2～3 个固定机位原始 1080P/4K 视频及至少一个标准双打视频。
- [ ] 保存当前基线：852×480 视频共 25,931 个检测记录，上半场球员有效率 76.4%，下半场球员 99.5%，羽毛球 87.0%。
- [ ] 保存对照基线：现有 720P 演示视频上半场球员有效率 99.1%，下半场球员 100%，羽毛球 95.5%。
- [ ] 后台日志和结果元数据分别记录：模型原始候选数、球场过滤后的候选数、轨迹过滤后的候选数、关键点置信度、每个半场的漏检率和最长连续漏检帧数。
- [x] 已提供三方案同口径脚本，输出召回率、漏检率、最长连续漏检、误检样本和单帧耗时；结果位于 `outputs/far_player_baseline/`。
- [ ] 接入跟踪器后再在同一测试集增加 ID-switch 和轨迹中断指标。

### P0：远端球员检测与持续追踪

- [x] YOLO Pose `imgsz` 支持 640、960、1280，并记录模型、阈值、设备、ROI、输入尺寸和检测来源。
- [x] 已实现全场 640 + 固定机位远端 ROI 640 二次检测及跨来源 NMS 合并，保留被合并来源。
- [x] 已完成初步三方案 CPU 基线。紧致标定 ROI 后，汇总召回率/平均耗时：全场640为0%/60.0ms，全场1280为95.8%/136.0ms，640+远端ROI为87.5%/118.8ms；当前默认推荐全场1280。
- [x] 人体落点已支持双脚中点、单脚和人体框底边中心三级规则，输出 `detected` / `detected_degraded` / `missing`、方法和置信度。
- [ ] 初步基线仍不足以接入 ByteTrack：低清1280召回率91.7%，单视频样本少于20，最长抽样漏检5秒，且尚未人工二次复核。
- [ ] 接入 ByteTrack，允许短暂漏检期间保持 `track_id` 和最后可信轨迹；预测位置必须标记为预测值，不能冒充模型真实检测。
- [ ] 双打视频中重点验证网前交叉、同队遮挡、跳跃和换位场景，记录每名球员的轨迹中断与 ID-switch。

### P1：羽毛球检测与时间轨迹

- [ ] 为羽毛球模型增加可配置的 `imgsz` 和固定机位分区检测，优先使用原始 1080P/4K 视频验证，避免依赖已经丢失细节的 480P 转码视频。
- [ ] 复测已知连续漏检片段。当前 25 帧样本中，640 没有产生候选球，1280 在 23/25 帧产生候选，CPU 耗时约从 75.4ms/帧增加到 200.2ms/帧。
- [ ] 不以继续降低当前 `0.18` 置信度阈值作为首选方案；必须同时衡量灯光、白线、鞋面和广告牌造成的误检。
- [ ] 增加基于速度、方向和可达范围的短时轨迹预测；输出需要区分 `detected`、`predicted`、`missing` 三种状态及置信度。
- [ ] 对远端和近端采用不同的尺寸、速度和跳变门限，并使用球场坐标或透视比例进行归一化，避免固定像素阈值对远端过严、对近端过松。

### P1：单双打通用数据模型

- [ ] 增加 `match_mode: singles | doubles`，单打每队最多一人，双打每队最多两人。
- [ ] 将 `upper/lower` 单人槽位重构为以 `track_id` 为键的 `players` 集合；半场只用于队伍归属，不能作为个人身份。
- [ ] 更新 `detections.jsonl`、元数据、轨迹、统计、热力图和散点图，支持个人、队伍和全场三个聚合层级。
- [ ] 增加开赛身份确认和人工纠错界面，将匿名 `track_id` 绑定到球员姓名，并支持换边后保持身份。
- [ ] 将单双打发球区规则与物理球场坐标分离：双打发球使用后发球线，回合进行中仍使用完整场地边界。

### P2：模型升级与训练决策门槛

- [ ] 先比较 `yolo11n-pose` 的高分辨率/分区方案、较大 YOLO Pose 模型、RTMPose/RTMO performance 模式，不直接开始训练。
- [ ] ByteTrack 的 ID-switch 超过验收门槛时，再对比 BoT-SORT；只有遮挡恢复仍明显不足时才评估 ReID。
- [ ] 只有在原始高清视频、合理推理尺寸、远端分区检测、后处理修正和时间跟踪全部完成后，远端球员或羽毛球指标仍不达标，才建立固定机位标注集并微调模型。
- [ ] 训练数据必须覆盖远端小目标、运动模糊、球网遮挡、跳跃、双打交叉、不同服装和灯光条件，并单独保留不参与训练的固定验证集与测试集。

### 完成定义

- [ ] 固定机位的新比赛无需重新点击四角，抽样校验通过后可直接分析。
- [ ] 同一个机位下，单打和双打共用一份球场标定配置，模式切换不会改变物理坐标。
- [ ] 所有位置结果都能追溯到真实检测或带置信度的预测状态。
- [ ] 在固定测试集上给出准确率、漏检率、最长中断、ID-switch、误检率和耗时对比报告后，才能关闭对应 TODO。

## 验收指标

双打支持不应只以“画面里出现四个人”为验收条件。至少应记录：

| 指标 | 含义 |
|---|---|
| 四人有效跟踪率 | 双打回合中，四名球员均持续拥有有效轨迹的帧比例。 |
| ID-switch 次数 | 同一人被错误分配为另一条 `track_id` 的次数。 |
| 漏检率 | 可见球员未被检测到的比例。 |
| 轨迹中断时长 | 单条球员轨迹因遮挡或漏检中断的帧数。 |
| 击球归属准确率 | 击球事件是否正确归属到四名球员中的一人。 |
| 队伍归属准确率 | 球员在换边、遮挡和临时跨区情况下是否仍归属于正确队伍。 |

建议先以固定全场机位的标准双打视频验证以上指标，再逐步扩展到侧拍、移动机位和复杂遮挡素材。

## 参考资料

- [Ultralytics 多目标跟踪文档](https://docs.ultralytics.com/modes/track/)
- [ByteTrack: Multi-Object Tracking by Associating Every Detection Box](https://arxiv.org/abs/2110.06864)
- [BoT-SORT: Robust Associations Multi-Pedestrian Tracking](https://arxiv.org/abs/2206.14651)
