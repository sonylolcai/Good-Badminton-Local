# 球路复核与人工标注工作流

## 目标

把一次完整比赛的原始检测结果转成可逐拍复核的球路样本。当前第一版支持高远球、挑球、吊球、杀球、平高/平抽球、网前球、发球、其他和不确定；它服务于人工确认与后续数据集积累，不直接产出比分、失误率或能力评分。

## 进入方式

WebUI 的“球路复核”Tab 会列出 `outputs/**/detections.jsonl` 所在的分析结果目录。选择一场已完成的分析结果，点击“打开整场复核”；左侧播放完整的“原视频人物 + 骨架 + 羽毛球标注”视频，右侧随播放时间显示最近一次触球的当前判断。需要修正时点击“编辑当前触球”；漏检时暂停或拖到对应画面，点击“在当前时间添加触球”，系统会记录精确时间点。旧结果若仅有 `web_skeleton_video.mp4`，页面会明确提示它不含真人画面。

`detections.jsonl` 目前没有保存每个关节的完整关键点，因此不能把一份旧的 Skeleton only 视频无损转换成“真人 + 完整骨架”视频。需要这种复核视频时，必须重新渲染该视频。

## 三层数据，不互相覆盖

| 层级 | 文件 | 是否可编辑 | 用途 |
|---|---|---:|---|
| 原始模型证据 | `detections.jsonl` | 否 | 每帧人物、球、轨迹和击球候选。可追溯、不可被人工标注污染。 |
| 机器候选与当前复核状态 | `shot_review/shot_candidates.json` | 由应用更新 | 对相邻候选去重，计算二维轨迹证据，保存当前人工结论。 |
| 人工审计记录 | `shot_review/annotations.jsonl` | 仅追加 | 每一次人工确认、修正、不确定或排除决定，保留复核人和备注。 |

片段缓存位于 `shot_review/clips/`。它是可重新生成的展示资产，不是训练真值。

## 自动建议的边界

第一版优先读取 `spatial.hit_events` 中已有的 `candidate`，并补充两类高召回二维轨迹信号：局部方向明显反转（`trajectory_turn`）和短暂断检后的轨迹衔接（`trajectory_gap_transition`）。没有这些字段的旧结果才使用“球靠近可见手部”的回退规则。相邻 0.40 秒内的候选会合并，优先保留空间事件、方向反转、断检衔接、手部接近的顺序。

建议只依据击球后 2D 羽毛球观测的速度、位移、观测时长和已有候选置信度。它的最高置信度被限制为 0.55，并始终标记 `needs_human_review`。高召回模式会产生“不是击球”的假候选，不能把候选总数当作击球总数。单目固定机位的二维轨迹不能可靠区分所有高远、吊球和杀球，尤其是在羽毛球断检、遮挡或视角变化时；低质量候选默认给出“不确定”。

因此，只有人工标为 `confirmed` 或 `corrected` 的样本，才可在未来进入球种统计、训练集或评测集。`uncertain` 和 `excluded` 保留为质量数据，不能被当成反例或运动员失误。

## 人工复核步骤

1. 打开整场视频并拖动到触球附近；右侧卡片会显示截至当前时间最近一次触球的自动建议或人工结论。
2. 如果自动候选正是当前触球，点击“编辑当前触球”；如果系统漏掉该拍，点击“在当前时间添加触球”。
3. 选择球种与复核结果：确认建议、人工修正、仍不确定或排除；填写复核人和备注。
4. 保存。页面会提示保存位置；原始 `detections.jsonl` 不会被写入。

人工添加会以当前视频播放时间创建 `manual_timeline` 候选，之后和自动候选一样可修改、保存和追溯。触球记录表保留自动候选与人工添加的时间点；点击任一行会按该条 `hit_time_sec`（秒）跳转整场播放器，而不是按帧号跳转。它用于核对与编辑，不会把完整比赛拆成独立播放器。被人工修改的内容只写入审计记录，原始检测不会被覆盖。

## 训练与评测使用原则

不要用一个视频的人工修正直接微调模型。积累多场、不同清晰度、FPS、固定机位角度和光照条件的数据；按“比赛”而非“帧”拆分训练/验证/测试，以避免同一场比赛相邻帧泄漏。开始训练前还需单独导出经人工确认的片段和标签，并记录所用模型版本、数据版本与评测指标。
